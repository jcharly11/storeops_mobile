name: storeops_mobile
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.9.0
  fluttertoast: ^8.2.12
  go_router: ^16.1.0
  shared_preferences: ^2.5.3
  provider: ^6.1.5
  dropdown_search: ^6.0.2
  cloud_firestore: ^6.0.0
  firebase_core: ^4.0.0
  intl: ^0.20.2
  firebase_messaging: ^16.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
