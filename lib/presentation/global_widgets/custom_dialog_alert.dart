import 'package:flutter/material.dart';

class CustomDialogAlert extends StatelessWidget {
  const CustomDialogAlert({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}