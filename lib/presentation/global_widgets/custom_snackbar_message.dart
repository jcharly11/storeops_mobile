import 'package:flutter/material.dart';

class CustomSnackbarMessage extends StatelessWidget {
  const CustomSnackbarMessage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}